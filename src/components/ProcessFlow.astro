---
const steps = [
  { title: "Requirement", desc: "Gathering specs and industry constraints." },
  { title: "Design", desc: "CAD development & validation." },
  { title: "Prototyping", desc: "Functional and fit testing." },
];
---

<section id="process" class="py-10 bg-gradient-to-br from-white via-blue-50 to-sky-100">
  <div class="max-w-6xl mx-auto px-6">
    <h2 class="text-4xl font-extrabold text-sky-700 drop-shadow mb-2">Our Process</h2>
    <p class="text-lg text-slate-600 mb-8 max-w-2xl">
      Our systematic workflow ensures precision, speed, and consistent quality.
    </p>

    <div class="grid md:grid-cols-3 gap-8">
      {steps.map((step) => (
        <div class="bg-white/70 rounded-2xl shadow-lg p-6 border border-blue-100 backdrop-blur-md">
          <h3 class="text-xl font-bold text-blue-600 mb-2">{step.title}</h3>
          <p class="text-slate-700 text-sm">{step.desc}</p>
        </div>
      ))}
    </div>

    <div class="mt-5">
      <a
        href="/process"
        class="px-8 py-3 bg-blue-600 text-white font-semibold rounded-xl shadow-lg hover:bg-blue-700 transition-all"
      >
        Read More →
      </a>
    </div>
  </div>
</section>
